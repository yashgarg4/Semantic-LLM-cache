# Pinned to versions verified working on Python 3.13 / Windows.
# Install:  pip install -r requirements.txt  &&  pip install -e .

# --- Core cache engine (Phase 1) ---
numpy==2.5.0
faiss-cpu==1.14.3
sentence-transformers==5.6.0
pydantic==2.13.4
python-dotenv==1.2.2

# --- OpenAI-compatible proxy (Phase 3) ---
fastapi==0.138.1
uvicorn[standard]==0.49.0

# --- Metrics dashboard (Phase 4) ---
streamlit==1.58.0

# --- Gemini-backed demo / proxy (Phase 3+) ---
langchain-google-genai==4.2.5

# --- Tests ---
pytest==9.1.1

# Note: torch (2.12.1) and transformers (5.12.1) are pulled in transitively by
# sentence-transformers and are not pinned here directly.
