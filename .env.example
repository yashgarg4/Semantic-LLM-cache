# Copy to .env and fill in. Only needed for the Gemini-backed demo/proxy
# (Phase 3+). The Phase 1 core cache and tests require no API key.
GOOGLE_API_KEY=

# Optional cache overrides (all have sensible defaults in CacheConfig):
# SEMCACHE_THRESHOLD=0.92
# SEMCACHE_EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
# SEMCACHE_MAX_ENTRIES=1000
